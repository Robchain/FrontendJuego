import React from "react"
const RompecabezaIndividualColor = ({numbera, url, alt}) => {
    return (
      <>  
          <img src={url} alt={alt} width={numbera} style={{display:'hidden'}} className={''}/>
      </>
    )
  }
  
  export default RompecabezaIndividualColor